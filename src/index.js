import React from 'react';
import ReactDOM from 'react-dom/client';
import Entrypoint from './frontend/Entrypoint';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <Entrypoint />
);
